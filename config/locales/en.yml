# frozen_string_literal: true

en:
  models:
    file_resource:
      errors:
        tags:
          empty_array: 'There is no tags.'
          duplicated: 'Some tags are duplicated.'
          invalid_format: "Invalid tag name for %{tag_name}, should not include +, - or space characters."
  services:
    filter_tags:
      exceptions:
        invalid_page_number: 'Page number is invalid.'
